Import SnapKit
import UIKit

final class {{ module_info.name }}ViewController: BaseViewController<{{ module_info.name }}State, {{ module_info.name }}Event, {{ module_info.name }}ViewPresenter> {
    
    // MARK: Constants

    // MARK: Views

    // MARK: Properties

    var router: {{ module_info.name }}Router!

    // MARK: LifeCycle

    override func viewDidLoad() {
        super.viewDidLoad()
    }

    // MARK: - Setup
    
    override func setupUI() {
	setupLayout()
    }

    private func setupLayout() {

    }

    // MARK: - Update State
    
    override func update(with state: State) {
        
    }
    
    // MARK: - Handle Event
    
    override func handle(_ event: Event) {
        
    }

    // MARK: Actions

    // MARK: Methods

}
